-- [[ SIMPLE ROBLOX MOD MENU - UPDATED ]] --

-- 1. MEMBUAT UI (ANTARMUKA)
local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local Title = Instance.new("TextLabel")
local SpeedButton = Instance.new("TextButton")
local JumpButton = Instance.new("TextButton")
local FlyButton = Instance.new("TextButton")
local KillAllButton = Instance.new("TextButton") -- Tombol Baru

-- Mengatur agar UI masuk ke CoreGui
ScreenGui.Parent = game:GetService("CoreGui")
ScreenGui.ResetOnSpawn = false

-- Mengatur Kotak Menu Utama (Main Frame ditinggikan jadi 310 biar muat)
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
MainFrame.Position = UDim2.new(0.1, 0, 0.2, 0)
MainFrame.Size = UDim2.new(0, 200, 0, 310) 
MainFrame.Active = true
MainFrame.Draggable = true

-- Judul Menu
Title.Name = "Title"
Title.Parent = MainFrame
Title.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
Title.Size = UDim2.new(0, 200, 0, 40)
Title.Font = Enum.Font.SourceSansBold
Title.Text = "Delta Mod Menu"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 18

-- Tombol Speed 100
SpeedButton.Name = "SpeedButton"
SpeedButton.Parent = MainFrame
SpeedButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
SpeedButton.Position = UDim2.new(0, 10, 0, 60)
SpeedButton.Size = UDim2.new(0, 180, 0, 40)
SpeedButton.Font = Enum.Font.SourceSans
SpeedButton.Text = "Speed 100: OFF"
SpeedButton.TextColor3 = Color3.fromRGB(255, 255, 255)
SpeedButton.TextSize = 16

-- Tombol Lompat Tinggi
JumpButton.Name = "JumpButton"
JumpButton.Parent = MainFrame
JumpButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
JumpButton.Position = UDim2.new(0, 10, 0, 120)
JumpButton.Size = UDim2.new(0, 180, 0, 40)
JumpButton.Font = Enum.Font.SourceSans
JumpButton.Text = "High Jump: OFF"
JumpButton.TextColor3 = Color3.fromRGB(255, 255, 255)
JumpButton.TextSize = 16

-- Tombol Melayang (Fly)
FlyButton.Name = "FlyButton"
FlyButton.Parent = MainFrame
FlyButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
FlyButton.Position = UDim2.new(0, 10, 0, 180)
FlyButton.Size = UDim2.new(0, 180, 0, 40)
FlyButton.Font = Enum.Font.SourceSans
FlyButton.Text = "Fly: OFF"
FlyButton.TextColor3 = Color3.fromRGB(255, 255, 255)
FlyButton.TextSize = 16

-- Tombol Kill All (Baru)
KillAllButton.Name = "KillAllButton"
KillAllButton.Parent = MainFrame
KillAllButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
KillAllButton.Position = UDim2.new(0, 10, 0, 240)
KillAllButton.Size = UDim2.new(0, 180, 0, 40)
KillAllButton.Font = Enum.Font.SourceSans
KillAllButton.Text = "Kill All: OFF"
KillAllButton.TextColor3 = Color3.fromRGB(255, 255, 255)
KillAllButton.TextSize = 16


-- 2. LOGIKA DAN FUNGSI FITUR

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local SpeedToggle = false
local JumpToggle = false
local FlyToggle = false
local KillAllToggle = false
local BodyVelocity = nil

game:GetService("RunService").RenderStepped:Connect(function()
    local Character = LocalPlayer.Character
    if Character and Character:FindFirstChild("Humanoid") then
        -- Logika Speed
        if SpeedToggle then
            Character.Humanoid.WalkSpeed = 100
        end
        
        -- Logika Jump
        if JumpToggle then
            Character.Humanoid.UseJumpPower = true
            Character.Humanoid.JumpPower = 150
        end
        
        -- Logika Melayang (Fly)
        if FlyToggle then
            local RootPart = Character:FindFirstChild("HumanoidRootPart")
            if RootPart then
                if not BodyVelocity then
                    BodyVelocity = Instance.new("BodyVelocity")
                    BodyVelocity.MaxForce = Vector3.new(400000, 400000, 400000)
                    BodyVelocity.Parent = RootPart
                end
                BodyVelocity.Velocity = Character.Humanoid.MoveDirection * 50 + Vector3.new(0, 2, 0)
            end
        else
            if BodyVelocity then
                BodyVelocity:Destroy()
                BodyVelocity = nil
            end
        end

        -- Logika Kill All (Mendekati musuh otomatis & menyerang jika memegang senjata)
        if KillAllToggle and Character:FindFirstChild("HumanoidRootPart") then
            for _, player in pairs(Players:GetPlayers()) do
                if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") and player.Character:FindFirstChild("Humanoid") then
                    if player.Character.Humanoid.Health > 0 then
                        -- Mengaktifkan tool/senjata otomatis jika ada di backpack
                        local tool = LocalPlayer.Backpack:FindFirstChildOfClass("Tool")
                        if tool then
                            tool.Parent = Character
                        end
                        
                        -- Berpindah ke posisi musuh secara instan
                        Character.HumanoidRootPart.CFrame = player.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, 2)
                        
                        -- Mengayunkan/mengaktifkan senjata jika memegang tool
                        local equippedTool = Character:FindFirstChildOfClass("Tool")
                        if equippedTool then
                            equippedTool:Activate()
                        end
                        
                        task.wait(0.1) -- Jeda waktu teleportasi agar tidak crash
                    end
                end
            end
        end
    end
end)

-- Deteksi Klik Tombol Speed
SpeedButton.MouseButton1Click:Connect(function()
    SpeedToggle = not SpeedToggle
    if SpeedToggle then
        SpeedButton.Text = "Speed 100: ON"
        SpeedButton.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
    else
        SpeedButton.Text = "Speed 100: OFF"
        SpeedButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
            LocalPlayer.Character.Humanoid.WalkSpeed = 16
        end
    end
end)

-- Deteksi Klik Tombol Jump
JumpButton.MouseButton1Click:Connect(function()
    JumpToggle = not JumpToggle
    if JumpToggle then
        JumpButton.Text = "High Jump: ON"
        JumpButton.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
    else
        JumpButton.Text = "High Jump: OFF"
        JumpButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
            LocalPlayer.Character.Humanoid.JumpPower = 50
        end
    end
end)

-- Deteksi Klik Tombol Fly
FlyButton.MouseButton1Click:Connect(function()
    FlyToggle = not FlyToggle
    if FlyToggle then
        FlyButton.Text = "Fly: ON"
        FlyButton.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
    else
        FlyButton.Text = "Fly: OFF"
        FlyButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    end
end)

-- Deteksi Klik Tombol Kill All (Baru)
KillAllButton.MouseButton1Click:Connect(function()
    KillAllToggle = not KillAllToggle
    if KillAllToggle then
        KillAllButton.Text = "Kill All: ON"
        KillAllButton.BackgroundColor3 = Color3.fromRGB(170, 0, 0) -- Warna merah (tanda bahaya/menyerang)
    else
        KillAllButton.Text = "Kill All: OFF"
        KillAllButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    end
end)
