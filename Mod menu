-- [[ ROBLOX MOD MENU - REAL GOD MODE VERSION ]] --

local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local Title = Instance.new("TextLabel")
local SpeedButton = Instance.new("TextButton")
local JumpButton = Instance.new("TextButton")
local FlyButton = Instance.new("TextButton")
local GodButton = Instance.new("TextButton") 

ScreenGui.Parent = game:GetService("CoreGui")
ScreenGui.ResetOnSpawn = false

MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
MainFrame.Position = UDim2.new(0.1, 0, 0.2, 0)
MainFrame.Size = UDim2.new(0, 200, 0, 310) 
MainFrame.Active = true
MainFrame.Draggable = true

Title.Name = "Title"
Title.Parent = MainFrame
Title.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
Title.Size = UDim2.new(0, 200, 0, 40)
Title.Font = Enum.Font.SourceSansBold
Title.Text = "Delta Helper Menu v2"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 18

SpeedButton.Name = "SpeedButton"
SpeedButton.Parent = MainFrame
SpeedButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
SpeedButton.Position = UDim2.new(0, 10, 0, 60)
SpeedButton.Size = UDim2.new(0, 180, 0, 40)
SpeedButton.Font = Enum.Font.SourceSans
SpeedButton.Text = "Speed 100: OFF"
SpeedButton.TextColor3 = Color3.fromRGB(255, 255, 255)
SpeedButton.TextSize = 16

JumpButton.Name = "JumpButton"
JumpButton.Parent = MainFrame
JumpButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
JumpButton.Position = UDim2.new(0, 10, 0, 120)
JumpButton.Size = UDim2.new(0, 180, 0, 40)
JumpButton.Font = Enum.Font.SourceSans
JumpButton.Text = "High Jump: OFF"
JumpButton.TextColor3 = Color3.fromRGB(255, 255, 255)
JumpButton.TextSize = 16

FlyButton.Name = "FlyButton"
FlyButton.Parent = MainFrame
FlyButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
FlyButton.Position = UDim2.new(0, 10, 0, 180)
FlyButton.Size = UDim2.new(0, 180, 0, 40)
FlyButton.Font = Enum.Font.SourceSans
FlyButton.Text = "Fly: OFF"
FlyButton.TextColor3 = Color3.fromRGB(255, 255, 255)
FlyButton.TextSize = 16

GodButton.Name = "GodButton"
GodButton.Parent = MainFrame
GodButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
GodButton.Position = UDim2.new(0, 10, 0, 240)
GodButton.Size = UDim2.new(0, 180, 0, 40)
GodButton.Font = Enum.Font.SourceSans
GodButton.Text = "Bypass Laser: OFF"
GodButton.TextColor3 = Color3.fromRGB(255, 255, 255)
GodButton.TextSize = 16

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local SpeedToggle = false
local JumpToggle = false
local FlyToggle = false
local GodToggle = false
local BodyVelocity = nil

-- Fungsi untuk menghapus detektor laser di sekitar karakter
local function BypassLasers()
    for _, v in pairs(workspace:GetDescendants()) do
        if v:IsA("TouchTransmitter") and v.Parent and (v.Parent.Name == "KillBrick" or v.Parent.Name == "Laser" or v.Parent.Name == "lava") then
            v:Destroy() -- Menghancurkan sensor mematikan pada laser
        end
    end
end

game:GetService("RunService").RenderStepped:Connect(function()
    local Character = LocalPlayer.Character
    if Character and Character:FindFirstChild("Humanoid") then
        if SpeedToggle then Character.Humanoid.WalkSpeed = 100 end
        if JumpToggle then 
            Character.Humanoid.UseJumpPower = true
            Character.Humanoid.JumpPower = 150 
        end
        if FlyToggle then
            local RootPart = Character:FindFirstChild("HumanoidRootPart")
            if RootPart then
                if not BodyVelocity then
                    BodyVelocity = Instance.new("BodyVelocity")
                    BodyVelocity.MaxForce = Vector3.new(400000, 400000, 400000)
                    BodyVelocity.Parent = RootPart
                end
                BodyVelocity.Velocity = Character.Humanoid.MoveDirection * 50 + Vector3.new(0, 2, 0)
            end
        else
            if BodyVelocity then BodyVelocity:Destroy() BodyVelocity = nil end
        end
        
        -- Jika tombol aktif, terus-menerus matikan sensor laser baru yang muncul
        if GodToggle then
            BypassLasers()
        end
    end
end)

SpeedButton.MouseButton1Click:Connect(function()
    SpeedToggle = not SpeedToggle
    SpeedButton.Text = SpeedToggle and "Speed 100: ON" or "Speed 100: OFF"
    SpeedButton.BackgroundColor3 = SpeedToggle and Color3.fromRGB(0, 170, 0) or Color3.fromRGB(50, 50, 50)
    if not SpeedToggle and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid.WalkSpeed = 16
    end
end)

JumpButton.MouseButton1Click:Connect(function()
    JumpToggle = not JumpToggle
    JumpButton.Text = JumpToggle and "High Jump: ON" or "High Jump: OFF"
    JumpButton.BackgroundColor3 = JumpToggle and Color3.fromRGB(0, 170, 0) or Color3.fromRGB(50, 50, 50)
    if not JumpToggle and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid.JumpPower = 50
    end
end)

FlyButton.MouseButton1Click:Connect(function()
    FlyToggle = not FlyToggle
    FlyButton.Text = FlyToggle and "Fly: ON" or "Fly: OFF"
    FlyButton.BackgroundColor3 = FlyToggle and Color3.fromRGB(0, 170, 0) or Color3.fromRGB(50, 50, 50)
end)

GodButton.MouseButton1Click:Connect(function()
    GodToggle = not GodToggle
    GodButton.Text = GodToggle and "Bypass Laser: ON" or "Bypass Laser: OFF"
    GodButton.BackgroundColor3 = GodToggle and Color3.fromRGB(0, 170, 0) or Color3.fromRGB(50, 50, 50)
    if GodToggle then
        BypassLasers()
    end
end)
